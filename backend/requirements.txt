fastapi>=0.110
uvicorn[standard]>=0.27
sqlalchemy>=2.0
pydantic>=2.6
PyJWT>=2.8
bcrypt>=4.1
psutil>=5.9
httpx>=0.27
python-multipart>=0.0.9
PyYAML>=6.0
nbtlib>=2.0
# Litematica 投影解析(.litematic → 指令清单)
litemapy>=0.9
# 被管理的 MCDR 实例以 `<python> -m mcdreforged` 启动;装进同一环境,
# 默认即用后端自身的解释器(见 config.DEFAULT_PYTHON_EXECUTABLE),开箱即用。
mcdreforged>=2.13
websockets
